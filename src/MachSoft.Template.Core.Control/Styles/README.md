# Styles

La hoja pública empaquetada se publica en `wwwroot/css/machsoft-template-core-control.css`.
Los estilos deben tokenizarse con variables `--ms-*`/`--mx-*` y evitar hardcodear color cuando exista token.
