@namespace MachSoft.Template.Core.Components.Foundation

<section class="ms-mx-empty-state" role="region" aria-label="@Title">
    @if (!string.IsNullOrWhiteSpace(Icon))
    {
        <div class="ms-mx-empty-state__icon" aria-hidden="true">@Icon</div>
    }

    <h3>@Title</h3>

    @if (!string.IsNullOrWhiteSpace(Description))
    {
        <p>@Description</p>
    }

    @if (Actions is not null)
    {
        <div class="ms-mx-empty-state__actions">
            @Actions
        </div>
    }
</section>

@code {
    [Parameter, EditorRequired] public string Title { get; set; } = string.Empty;
    [Parameter] public string? Description { get; set; }
    [Parameter] public string? Icon { get; set; }
    [Parameter] public RenderFragment? Actions { get; set; }
}
